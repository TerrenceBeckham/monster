<% @places.each do |place| %>
<div class="booyah-box col-xs-10 col-xs-offset-1"> <!-- Left "col" sets the left alignment and right "col" sets the right alignment -->
  <h1><%= link_to place.name, place_path(place) %></h1><br />  <!-- This code sends all clicks on the link to the show page -->
  <i><%= place.address %></i><br /><br />
  <p><%= place.description %></p><br />

  <% if place.user.present? %>
    <small><%= place.user.email %></small>
  <% end %>

 <% if place.comments.present? %>
  <div class="col-xs-12">
    <blockquote>
      <% if place.comments.last.humanized_rating.present? %>
        <b><%= place.comments.last.humanized_rating %></b> -
      <% end %>
      <%= place.comments.last.message %>
      <small><%= place.comments.last.user.email %></small>
    </blockquote>
  </div>
<% end %>
</div>

 <%= paginate @places %>
